image: alpine/edge
sources:
  - git@git.sr.ht:~slondr/diarchy
packages:
  - elixir
tasks:
  - build: |
      cd diarchy
      mix compile --warnings-as-errors
  - test: |
      cd diarchy
      mix test
triggers:
  - action: email
    condition: failure
    to: Eric S. Londres <ericlondres@outlook.com>

      
